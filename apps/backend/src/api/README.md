# API layer

HTTP handlers and route definitions for the Briefly backend.

Edge Functions live under `supabase/functions/`. This directory is reserved for
shared API utilities, request/response types, and future service boundaries as
the backend grows beyond Supabase Edge Functions.
