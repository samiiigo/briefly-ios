# Root scripts

| Script                       | Purpose                                                             |
| ---------------------------- | ------------------------------------------------------------------- |
| `rewrite-mobile-imports.mjs` | One-time helper used during monorepo migration (kept for reference) |

Day-to-day commands live in the root `package.json` (`pnpm typecheck`, `pnpm lint`, …).
