# Root scripts

Prefer workspace scripts from the repository root (`pnpm typecheck`, `pnpm dev:mobile`, etc.).

Mobile-specific scripts (EAS env, Supabase deploy, llama Windows install, test runner) remain under [`apps/mobile/scripts/`](../apps/mobile/scripts/).
