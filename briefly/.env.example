# Copy to .env and fill in for local development (never commit .env).
# EAS Build: create matching secrets with `eas secret:create --scope project`.

EXPO_PUBLIC_ASSEMBLYAI_API_KEY=
EXPO_PUBLIC_OPENROUTER_API_KEY=

# Optional non-public fallbacks (also settable as EAS secrets without EXPO_PUBLIC_ prefix):
# ASSEMBLYAI_API_KEY=
# OPENROUTER_SHARED_API_KEY=
