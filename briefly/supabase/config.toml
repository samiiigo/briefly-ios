# Linked to: https://supabase.com/dashboard/project/vcuvfstcobxujjrvqpop
# Apple + Google providers are enabled in the Supabase dashboard (Auth → Providers).
# Keep redirect URLs in sync with the app deep link briefly://auth/callback.
[project]
id = "vcuvfstcobxujjrvqpop"
name = "briefly"

[api]
enabled = true

[db]
major_version = 15

[storage]
enabled = true

[auth]
enabled = true
site_url = "briefly://auth/callback"
additional_redirect_urls = ["briefly://auth/callback"]

[functions]
verify_jwt = true
