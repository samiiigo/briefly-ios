/** Maximum user folders that can be pinned to the Library home screen. */
export const MAX_PINNED_FOLDERS = 6;

/** Maximum unpinned user folders previewed under Your folders on Library. */
export const MAX_YOUR_FOLDERS_PREVIEW = 6;

/** Full-list screen opened from Library “See all”. */
export type UserFolderListFilter = 'pinned' | 'all-user';
