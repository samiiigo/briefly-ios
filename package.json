{
  "name": "briefly",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace @briefly/shared",
    "test:shared": "npm run test --workspace @briefly/shared"
  }
}
